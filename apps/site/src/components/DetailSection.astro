---
interface Props {
  title: string;
  open?: boolean;
}

const { title, open = false } = Astro.props;
---

<details class="unit-section" open={open}>
  <summary>{title}</summary>
  <slot />
</details>
