---
type NavSection = 'ai-prompt' | 'ks3' | 'igcse' | 'ib';

interface Props {
  activeSection?: NavSection;
}

const { activeSection } = Astro.props;

const sections = [
  { key: 'ai-prompt' as const, label: 'AI Prompt', href: '/ai-prompt-generator/index.html' },
  { key: 'ks3' as const, label: 'KS3', href: '/ks3/index.html' },
  { key: 'igcse' as const, label: 'IGCSE', href: '/igcse/index.html' },
  { key: 'ib' as const, label: 'IB', href: '/ib-2027/index.html' }
];
---

<header class="site-header main-header">
  <div class="site-header-inner header-primary">
    <div class="logo-container">
      <a href="/index.html" class="site-brand" aria-label="SGS Education home">
        <img
          src="/images/Logo.png"
          alt="SGS Education logo"
          class="site-brand-logo logo-img"
          width="40"
          height="40"
          loading="eager"
          fetchpriority="high"
        />
        <span class="site-title">SGS Education</span>
      </a>
    </div>

    <nav class="site-primary-nav main-nav main-nav-simple" aria-label="Main navigation">
      {sections.map((section) => (
        <a
          href={section.href}
          class:list={{ 'nav-link': true, active: activeSection === section.key }}
          aria-current={activeSection === section.key ? 'page' : undefined}
        >
          {section.label}
        </a>
      ))}
    </nav>

    <div class="site-auth-slot" data-auth-slot aria-label="Account actions" />
  </div>
</header>
