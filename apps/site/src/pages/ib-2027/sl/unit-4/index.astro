---
import IbUnitIndexLayout from '../../../../components/IbUnitIndexLayout.astro';
import { getIbSlUnitIndex, getIbSlUnitPlan } from '@sgs/content-schema';

const page = await getIbSlUnitIndex(4);
const unitPlan = await getIbSlUnitPlan(4);
---

<IbUnitIndexLayout page={page} unitPlan={unitPlan} />
