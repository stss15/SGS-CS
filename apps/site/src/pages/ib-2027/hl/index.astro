---
import HeroPanel from '../../../components/HeroPanel.astro';
import TopicSectionGrid from '../../../components/TopicSectionGrid.astro';
import BaseLayout from '../../../layouts/BaseLayout.astro';
import { getIbHlCurriculumIndex } from '@sgs/content-schema';

const page = await getIbHlCurriculumIndex();
---

<BaseLayout
  title={page.title}
  description={page.description || 'IB Computer Science Higher Level units for first assessment 2027.'}
  activeSection={page.activeSection}
  breadcrumbs={page.breadcrumbs}
>
  <HeroPanel
    eyebrow="IB 2027"
    title={page.hero.title}
    subtitle={page.hero.subtitle}
  >
    <TopicSectionGrid sections={page.sections} />
  </HeroPanel>
</BaseLayout>
