interface:
  display_name: "SGS Architecture Map"
  short_description: "Map the SGS refactor architecture"
  default_prompt: "Use $site-architecture-map to locate source-of-truth files, route ownership, and the right subsystem for this SGS refactor task."
