{
  "name": "computer_science_app-copy",
  "version": "1.0.0",
  "description": "",
  "main": "scripts/build.js",
  "scripts": {
    "build": "node scripts/build.js",
    "serve": "firebase emulators:start --only hosting",
    "deploy": "npm run build && firebase deploy --only hosting",
    "preview": "npm run build && firebase hosting:channel:deploy preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "fast-glob": "^3.3.3",
    "fs-extra": "^11.3.2",
    "gray-matter": "^4.0.3",
    "nunjucks": "^3.2.4"
  }
}