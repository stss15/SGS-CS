# Level D: Item Use

Add `consume()` to Inventory and `use_item()` to Player.

## Files to Edit
- `student/inventory.py` — add consume()
- `student/player.py` — add use_item()

## Validate
```
python3 validate.py
```
