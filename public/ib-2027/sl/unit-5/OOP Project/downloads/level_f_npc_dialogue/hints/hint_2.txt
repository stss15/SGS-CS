=== HINT 2: Input Handling Pattern ===

Use a simple input-validation loop:
1) Ask for a number
2) Convert to int (handle non-numeric input)
3) Subtract 1 to make it 0-based
4) Check the range (0 <= index < len(options))
5) If valid, return it; if not, ask again

Remember: the engine already printed the options.
