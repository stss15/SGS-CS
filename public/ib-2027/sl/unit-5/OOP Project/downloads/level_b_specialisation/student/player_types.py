from student.player import Player

class Brute(Player):

    # GET STARTING STATS (override)
    get_starting_stats()


class Scout(Player):

    # GET STARTING STATS (override)
    get_starting_stats()
