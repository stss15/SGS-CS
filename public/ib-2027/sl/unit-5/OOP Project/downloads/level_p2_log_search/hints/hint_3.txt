=== HINT 3: Detailed Guidance ===

LOAD_LOGS_FROM_FILE STRUCTURE:

def load_logs_from_file(filepath):
    logs = []
    
    # Open and read the file
    with open(filepath, "r") as f:
        lines = f.readlines()
    
    # Skip header line (first line)
    for line in lines[1:]:
        # Strip whitespace and split by |
        parts = line.strip().split("|")
        
        # Create dictionary (if valid line)
        if len(parts) == 4:
            # Build log dictionary with the 4 fields
            # Keys: timestamp, author, corrupted, content
            pass
        
        # Add to logs list
    
    return logs

---------------------------------------------
SEARCH_LOGS STRUCTURE
---------------------------------------------

def search_logs(logs, author):
    # Filter by author - either use a loop
    # or a list comprehension
    pass

---------------------------------------------
SORT_LOGS STRUCTURE
---------------------------------------------

def sort_logs(logs, key, descending=False):
    # Use sorted() with a lambda for the key
    # Remember to handle the descending parameter
    pass

---------------------------------------------
COMMON MISTAKES
---------------------------------------------

✗ Forgetting to skip the header line
    → lines[1:] skips the first line

✗ Not stripping whitespace
    → line.strip() removes newlines

✗ Wrong dictionary keys
    → Must be: timestamp, author, corrupted, content

Run validate.py to check your work!
