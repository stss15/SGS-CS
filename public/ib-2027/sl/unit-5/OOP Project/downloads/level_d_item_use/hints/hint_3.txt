=== HINT 3: Method Structure ===

PLAYER.USE_ITEM PATTERN:

def use_item(self, item_name, target):
    # Step 1: Check if we have the item
    # → Use inventory.has_item()
    # → If not, return an error message
    
    # Step 2: Handle each item type differently
    
    # MED_PATCH:
    # → Only usable on "self"
    # → Call self.heal(amount) 
    # → Consume the item
    # → Return success message
    
    # RATION_PACK:
    # → Only usable on "self"
    # → Consume the item
    # → Return message
    
    # KEYCARD:
    # → Not consumed when used
    # → Can be used on any target
    # → Return message about swiping
    
    # TORCH:
    # → Not consumed
    # → Return message about shining light
    
    # UNKNOWN ITEM:
    # → Return a message saying you don't know how to use it

---------------------------------------------
KEY CONCEPTS
---------------------------------------------

✓ Check inventory BEFORE trying to use
✓ Different items have different behaviors
✓ Some items get consumed, some don't
✓ Always return a descriptive string
✓ Use if/elif/else to handle different item types

Run validate.py to check your work!
