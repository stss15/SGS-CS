=== HINT 2: The Stack Class ===

REQUIREMENTS:
  - Use a list for storage: self._items = []
  - Push adds to END of list (use append)
  - Pop removes from END of list (use list's pop method)

STRUCTURE (fill in the logic):

class Stack:
    def __init__(self):
        # Create empty list called _items
        pass
    
    def push(self, item):
        # Add item to end of list
        pass
    
    def pop(self):
        # Return None if empty
        # Otherwise remove and return last item
        pass
    
    def peek(self):
        # Return None if empty
        # Otherwise return last item WITHOUT removing
        # Hint: use index [-1]
        pass
    
    def is_empty(self):
        # Return True if no items, False otherwise
        pass
    
    def size(self):
        # Return number of items
        pass

---------------------------------------------
KEY HINTS
---------------------------------------------
  - List's append() adds to end
  - List's pop() removes from end (no argument needed)
  - Check is_empty() before trying to pop/peek

---------------------------------------------
WHY LIFO?
---------------------------------------------
  push(A), push(B), push(C)
  List is now: [A, B, C]
  
  pop() returns C  (last in, first out)
  List is now: [A, B]
