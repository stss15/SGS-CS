{% macro cardGrid(cards, basePath='./') %}
<div class="arcade-grid">
    {% for card in cards %}
        <a href="{{ card.href }}" class="game-card">
            {% if card.image %}
                <img src="{{ basePath }}{{ card.image }}" alt="{{ card.alt | default(card.title) }}" class="game-thumb">
            {% endif %}
            <div class="game-info">
                <h3 class="game-title">{{ card.title }}</h3>
                {% if card.subtitle %}<p class="game-type">{{ card.subtitle }}</p>{% endif %}
            </div>
        </a>
    {% endfor %}
    </div>
{% endmacro %}

{% macro resourceGrid(items, basePath='./') %}
<div class="resource-grid">
    {% for item in items %}
        {% set isProtected = item.password %}
        <a href="{{ item.href | default('#') }}" class="resource-btn"
            {% if item.target %}target="{{ item.target }}"{% if item.target == '_blank' %} rel="noopener"{% endif %}{% endif %}
            {% if item.onclick %}data-sgs-onclick="{{ item.onclick }}"{% endif %}
            {% if item.carouselKey %}data-carousel="{{ item.carouselKey }}"{% endif %}
            {% if item.carouselImages %}data-carousel-images="{{ item.carouselImages | join('||') }}"{% endif %}
            {% if isProtected %}data-protected-password="{{ item.password }}"{% endif %}
            {% if item.protectedMessage %}data-protected-message="{{ item.protectedMessage }}"{% endif %}>
            <span class="resource-number">
                {% if item.icon %}<i class="{{ item.icon }}"></i>{% else %}{{ item.number }}{% endif %}
            </span>
            <div class="resource-info">
                <span class="resource-name">{{ item.name }}</span>
                {% if item.type %}<span class="resource-type">{{ item.type }}</span>{% endif %}
            </div>
            <i class="{% if isProtected %}fa-solid fa-lock{% else %}fa-solid fa-arrow-right{% endif %} resource-arrow"></i>
        </a>
    {% endfor %}
</div>
{% endmacro %}

{% macro topicSections(sections, basePath='./', wrapperClass='') %}
<div class="papers-wrapper {{ wrapperClass }}">
    {% for section in sections %}
        <section class="paper-section">
            <div class="paper-header">
                <h2 class="paper-title">{{ section.title }}</h2>
                {% if section.subtitle %}<p class="paper-subtitle">{{ section.subtitle }}</p>{% endif %}
            </div>
            <ul class="topic-list">
                {% for topic in section.items %}
                    <li>
                        <a href="{{ topic.href }}" class="topic-item">
                            <span class="topic-number">{{ topic.number }}</span>
                            <span class="topic-name">{{ topic.name }}</span>
                            <i class="fa-solid fa-chevron-right topic-arrow"></i>
                        </a>
                    </li>
                {% endfor %}
            </ul>
        </section>
    {% endfor %}
</div>
{% endmacro %}

{% macro featureCards(cards) %}
<div class="cards-container">
    {% for card in cards %}
        <a href="{{ card.href }}" class="card" {% if card.id %}id="{{ card.id }}"{% endif %}>
            {% if card.icon %}
                <div class="card-icon"><i class="{{ card.icon }}"></i></div>
            {% endif %}
            <h2 class="card-title">{{ card.title }}</h2>
            {% if card.desc %}<p class="card-desc">{{ card.desc }}</p>{% endif %}
        </a>
    {% endfor %}
</div>
{% endmacro %}

{% macro independentTaskGrid(items=[], basePath='./') %}
<div class="activity-grid independent-grid">
    {% for item in items %}
        <a href="{{ item.href }}" class="activity-card independent-card {% if not item.image %}no-image{% endif %}"
            {% if item.target %}target="{{ item.target }}"{% if item.target == '_blank' %} rel="noopener"{% endif %}{% endif %}>
            {% if item.image %}
                {% set imageSrc = item.image %}
                {% if item.image.indexOf('/') != 0 and item.image.indexOf('http') != 0 and item.image.indexOf('//') != 0 %}
                    {% set imageSrc = basePath ~ item.image %}
                {% endif %}
                <img src="{{ imageSrc }}" alt="{{ item.alt | default(item.title) }}" class="activity-thumb">
                <div class="activity-info-overlay">
                    <span class="activity-label">{{ item.title }}</span>
                    {% if item.subtitle %}<span class="igcse-task-type">{{ item.subtitle }}</span>{% endif %}
                </div>
            {% else %}
                <span class="activity-number"><i class="{{ item.icon | default('fa-solid fa-cube') }}"></i></span>
                <span class="activity-label">{{ item.title }}</span>
                {% if item.subtitle %}<span class="igcse-task-type">{{ item.subtitle }}</span>{% endif %}
            {% endif %}
        </a>
    {% endfor %}
</div>
{% endmacro %}
