# IGCSE Textbook Source Map: Topic 3 - Hardware

## Scope
- Topic: 3
- Topic name: Hardware
- Source policy: igcse_textbook_then_syllabus_then_slides
- Source hierarchy: chapter text files -> Cambridge syllabus -> existing SGS slides (fallback only).

## Canonical Website Subtopics
- 3.1 Computer Architecture
- 3.2 Input & Output Devices
- 3.3 Data Storage
- 3.4 Network Hardware
- 3.5 Review & Exam Prep

## Mapped Source Files
- docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.1.txt
- docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.2.txt
- docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.3.txt
- docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.4.txt
- docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.5.txt
- docs/content/igcse/chapter-text-files/Chapter 3 key words.txt

## Locked Noise Exclusions
- docs/content/igcse/chapter-text-files/Chapter 5 Subfiles/5123.txt
- docs/content/igcse/chapter-text-files/chapter 3 Subfiles/300.txt
- docs/content/igcse/chapter-text-files/Chapter 7 Subfiles/780340.txt
- docs/content/igcse/chapter-text-files/.DS_Store

## Evidence by Subtopic
### 3.1 Computer Architecture
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.1.txt`
```text
data in a computer application. As Figure 3.1 shows, the CPU consists of:
features (none of which were available in computers prior to the mid-1940s):
summarised in Table 3.1. The use of these registers is explained more fully in the
Earlier on, Figure 3.1 referred to some components labelled as buses. Figure 3.2
and 8 bits for the content. In a real computer memory, the address and its
to all the other computer components. It is usually 8-bits wide. There is no real
4 The use of a different number of cores can improve computer performance.
1 a Name three buses used in the von Neumann architecture.
```
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.2.txt`
```text
in Figure 3.15. Note there are different patterns for digits on the left-hand side
in Figure 3.17 is a website advertising rock music merchandise. It includes a web
advertising products (for example, the QR code in Figure 3.17)
storing boarding passes electronically at airports and train stations (Figure 3.18).
increased ability to add advertising logos (see Figure 3.19). Frame QR codes come
1 Using the data in Figure 3.14, design the barcodes for:
The following diagram (Figure 3.25) and description summarises how the computer
As Figure 3.46 shows, if the sound is stored in a computer file, it must pass
```
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.3.txt`
```text
Finally, Table 3.11 summarises the main differences between CDs, DVDs and Blu-ray.
are shown in Table 3.12.
b Five programs are currently being run in a computer. Program 1 is using
3 Five descriptions of computer terms are shown on the left and five terms
All computers require some form of memory and storage. Memory is usually
referred to as the internal devices used to store data that the computer can
user's workspace, temporary data or data that is key to running the computer.
allow data, for example, to be transferred between computers. Removable devices
```
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.4.txt`
```text
be transferred to various computers throughout the network.
computer on the internet, configured as a DHCP server, is used by the ISP to
several cables connecting to computers and other devices on the LAN.
Broadband routers sit behind a firewall. The firewall protects the computers on
Routers inspect the data package sent to it from any computer on any of the
networks connected to it. Since every computer on the same network has the
insight into further study of computer hardware and
Input and output devices are connected to a computer
```
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.5.txt`
```text
Cambridge O Level Computer Science 2210, Paper 12 Q9, Oct/Nov 2017
Cambridge O Level Computer Science 0478, Paper 12 Q3, May/June 2017
in a computer that can carry data, addresses or control
control unit to all other computer components
word – a group of bits used by a computer to represent a
single unit; for example, modern computers often use
computer system during the boot-up process
form understood by a computer
```
- Source: `docs/content/igcse/chapter-text-files/Chapter 3 key words.txt`
```text
von Neumann architecture – a type of computer
architecture which introduced the concept of the stored
control unit – the component of a computer's CPU that
throughout the computer by sending out control signals
in a computer that can carry data, addresses or control
control unit to all other computer components
word – a group of bits used by a computer to represent a
single unit; for example, modern computers often use
```

### 3.2 Input & Output Devices
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.2.txt`
```text
several other input and output devices at the checkout:
used as the input devices on computers, tablets, mobile phones and many other
Loudspeakers are output devices that produce sound. When connected to a
Sensors are input devices which read or measure physical properties from
arranged differently as shown in Figure 3.24. They are also designed to give more
The following diagram (Figure 3.25) and description summarises how the computer
In Figure 3.25, the 'H' key is pressed and this completes a circuit as shown
through the USB port or using Bluetooth connectivity. Figure 3.26 shows how
```
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.1.txt`
```text
The input and output devices will be covered in more detail in Section 3.2.
and to and from input/output devices. It is important to point out that data can
computer systems. Input devices convert external data into a form the computer
microphones). Output devices show the results of computer processing in a
accessing the BIOS (Basic Input/Output System) and altering the settings.
memory, input/output (I/O) ports and secondary
Earlier on, Figure 3.1 referred to some components labelled as buses. Figure 3.2
consists of an address and its contents. Table 3.2 uses 8 bits for each address
```
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.3.txt`
```text
powering up for the first time (the basic input/output system (BIOS)); these
storage. Figure 3.57 summarises the types of memory and storage devices covered
1 Describe how ROM and RAM chips could be used in the following devices:
referred to as the internal devices used to store data that the computer can
Storage devices allow users to store applications, data and files. The user's data
files (such as music files or videos). Storage devices can also be removable to
allow data, for example, to be transferred between computers. Removable devices
Memory and storage devices can be split up into two distinct groups:
```
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.4.txt`
```text
Input and output devices are connected to a computer
input and output is controlled by the control unit.
are all centred on a 2.45 GHz frequency. Devices using
devices which are very close together (< 30 metres
they are used to connect devices to the internet or other networks. However,
devices to fall into a strict format; because of this, it may be necessary to
change the MAC address of some devices to ensure they follow the correct
devices will be blocked if their MAC address doesn't adhere to the correct
```
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.5.txt`
```text
9 The diagram shows five output devices and five descriptions.
Basic Input/Output System (BIOS) – a suite of programs
from CPU to memory (and vice versa) or to and from input/
actuator – an output device that converts electrical energy
pointing device – an input device that allows the user to
loudspeaker – an output device that converts electric
memory – the devices within the computer that are directly
example, which are known as storage devices
```
- Source: `docs/content/igcse/chapter-text-files/Chapter 3 key words.txt`
```text
Basic Input/Output System (BIOS) – a suite of programs
from CPU to memory (and vice versa) or to and from input/
actuator – an output device that converts electrical energy
pointing device – an input device that allows the user to
loudspeaker – an output device that converts electric
memory – the devices within the computer that are directly
example, which are known as storage devices
data on several storage devices at the same time
```

### 3.3 Data Storage
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.3.txt`
```text
Storage devices allow users to store applications, data and files. The user's data
an application or program, data is retrieved from secondary storage and placed
continually access the secondary data storage devices to overwrite old data on
stored as long as required by the user. This type of storage can store more data
of solid state storage devices store data by controlling the movement of
Cloud storage is a method of data storage where data is stored on remote
Instead of saving data on a local hard disk or other storage device, a user can
save their data 'in the cloud'. The benefits and drawbacks of using cloud storage
```
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.1.txt`
```text
data in a computer application. As Figure 3.1 shows, the CPU consists of:
this data has to be written into location with address: 1111 1101; so this
1 The width of the address bus and data bus (as mentioned earlier) can also
Early computers were fed data while the machines were actually running; it wasn't
possible to store programs or data, which meant they couldn't operate without
computer memories could store programs as well as data
synchronisation of data flow and program instructions throughout the computer. A
The RAM holds all the data and programs needed to be accessed by the CPU. The RAM
```
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.2.txt`
```text
1 Using the data in Figure 3.14, design the barcodes for:
square represents a 0 or a 1. Calculate how many bytes of data can be
The face in Figure 3.30 shows several of the positions used by the face
3.32). Since human skin is a conductor of electricity, when bare fingers (or a
general structure as shown in Figure 3.32.
will move when a current is applied to the coil (see Figure 3.36). This would allow
if there is more data in the printer buffer, then the whole process from stage 5 is repeated until the buffer is
which is converted into digital data – this allows the computer to understand
```
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.4.txt`
```text
in the amount of data traffic. The main advantages of IPv6 compared to IPv4 are:
2 of the wires are used in the data transmission.
the rules that must be agreed by senders and receivers of data communicating
when a packet of data is sent and received,
less time to retrieve data)
Routers enable data packets to be routed between different networks, for
example, to join a LAN to a WAN. The router takes data transmitted in one format
from a network (which is using a particular protocol) and converts the data to a
```
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.5.txt`
```text
data on several storage devices at the same time
cloud storage – a method of data storage where data is
in a computer that can carry data, addresses or control
data bus – the system bus that allows data to be carried
and data are fetched from memory, decoded and finally
immediate access store (IAS) – memory that holds all data
backing store – a secondary storage device (such as HDD
or SSD) used to store data permanently even when the
```
- Source: `docs/content/igcse/chapter-text-files/Chapter 3 key words.txt`
```text
data on several storage devices at the same time
cloud storage – a method of data storage where data is
execution or processing of all the instructions and data in a
memory data register (MDR) – a register that stores data
that has just been read from memory or data that is about
ensures synchronisation of data flow and programs
in a computer that can carry data, addresses or control
data bus – the system bus that allows data to be carried
```

### 3.4 Network Hardware
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.4.txt`
```text
a network (such as the internet). It is usually part of the device hardware
Topic 2: Wired and wireless networks
A network interface card (NIC) is needed to allow a device to connect to
Wireless network interface cards/controllers (WNICs) are the same as NICs in that
they are used to connect devices to the internet or other networks. However,
networks via microwaves. They would normally plug into the USB port or be part
to get past certain types of network restrictions it may be necessary to
be changed on certain devices connected to the network.
```
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.1.txt`
```text
An embedded system is a combination of hardware and software which is
designed to carry out a specific set of functions. The hardware is electronic,
automatic updates via a Wi-Fi, satellite or cellular (mobile phone network) link
```
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.2.txt`
```text
The alloy wheel in Figure 3.42 was made using an industrial 3D printer.
to bend screens to any shape (see Figure 3.45). When this is
As Figure 3.46 shows, if the sound is stored in a computer file, it must pass
any other hardware devices you think
```
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.3.txt`
```text
hypervisor is a piece of computer software, firmware or hardware that creates
cloud accounts, which enabled them to publish the photos on social networks
```
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.5.txt`
```text
network interface card (NIC) – a hardware component
embedded system – a combination of hardware and
connect to a network, such as the internet
between different networks, for example, to join a LAN to
MAC address – a unique identifier which acts as a network
device each time it logs onto a network
```
- Source: `docs/content/igcse/chapter-text-files/Chapter 3 key words.txt`
```text
network interface card (NIC) – a hardware component
embedded system – a combination of hardware and
connect to a network, such as the internet
between different networks, for example, to join a LAN to
MAC address – a unique identifier which acts as a network
device each time it logs onto a network
```

### 3.5 Review & Exam Prep
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.5.txt`
```text
drawings (for example, to send to a 3D printer or to produce
single unit; for example, modern computers often use
data - other examples include web cache, database cache,
actuators and relays, for example
analogue data (for example, data read from sensors) into a
example, excessive clicking of a mouse button over a period
calculates, for example, the coordinates of a pointing device
example, which are known as storage devices
```
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.1.txt`
```text
typical current value is 3.5 GHz – which means 3.5 billion clock cycles a second).
One example of an instruction set is the X86, a common CPU standard used in
an MP3 player) to a complex array of multiple units (for example, in a medical
and on the internet. The following diagram is one example of a simple
Let us now consider two examples of how the MAR and MDR registers can be used
higher clock speed. Other factors need to be considered, for example:
lead to problems, for example:
For example, with a dual core the CPU communicates with both cores using
```
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.2.txt`
```text
For example, the OCR software is able to review these images, select the text
for example: the digit '3' on the left generates the pattern: L D D D D L D
advertising products (for example, the QR code in Figure 3.17)
(now in a digital format); for example, an 8-bit ADC gives 28 (256) possible
brightness levels per pixel (for example, brightness level 01110011)
to 255); for example, a shade of orange could be 215 (red), 165 (green) and 40
prepares it in a format that is understood by the 3D printer
system in more detail. Figure 3.51 concentrates on the sensor input.
```
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.3.txt`
```text
storage. Figure 3.57 summarises the types of memory and storage devices covered
of primary memory is shown in Figure 3.59.
allow data, for example, to be transferred between computers. Removable devices
Examples include HDD, SSD, DVD, memory
speed is essential, for example, in the CPU's memory cache, SRAM is the preferred
storing of the set routines; for example, how the buttons on the hand-held
(for example, documents, photos and music) are stored on secondary storage. The
when compared to, for example, RAM. Many applications
```
- Source: `docs/content/igcse/chapter-text-files/chapter 3 Subfiles/3.4.txt`
```text
For example, 00 – 1C – B3 – 4F – 25 – FF where the first six hex digits identify
the device as made by, for example, Apple and the second set of six hex digits
of hex digits; for example,
when using, for example, VoIP since this
example, to join a LAN to a WAN. The router takes data transmitted in one format
```
- Source: `docs/content/igcse/chapter-text-files/Chapter 3 key words.txt`
```text
drawings (for example, to send to a 3D printer or to produce
sends out pulses along the control bus; for example, a
single unit; for example, modern computers often use
data - other examples include web cache, database cache,
actuators and relays, for example
analogue data (for example, data read from sensors) into a
example, excessive clicking of a mouse button over a period
calculates, for example, the coordinates of a pointing device
```

